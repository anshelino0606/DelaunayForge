#include "volumetric_mesh_component.h"

namespace fem {

FEM_DEFINE_OBJECT(VolumetricMeshComponent, MeshComponent);

FEM_BEGIN_PROPERTY_REGISTER(VolumetricMeshComponent)
{
    
}
FEM_END_PROPERTY_REGISTER(VolumetricMeshComponent);

}